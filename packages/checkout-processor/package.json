{
  "name": "@bookipi/checkout-processor",
  "version": "0.0.0",
  "private": true,
  "description": "Lambda checkout handler for atomic Valkey reservations and SQS events.",
  "type": "module",
  "imports": {
    "#types": {
      "development": "./src/types.ts",
      "types": "./src/types.ts",
      "default": "./dist/types.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-sqs": "^3.1138.0",
    "ioredis": "^5.11.1",
    "zod": "4.1.12"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.163",
    "@types/node": "24.5.2",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  },
  "engines": {
    "node": ">=24 <25"
  }
}
